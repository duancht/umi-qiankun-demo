{
  "scripts": {
    "start": "npm-run-all --parallel start:*",
    "start:master": "cd main-app && npm start",
    "start:app": "cd sub-app && npm start"
  },

  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
